x = 0  # Dodaj pierwszy element do tablicy zmiennych
y = 3  # Dodaj drugi element do tablicy zmiennych

def fun(var1: int, var2: float):  # Utwórz tablicę zmiennych w analizatorze semantycznym i dodaj 2 zmienne do niej
    if x == var1:  # Znajdź pierwszą zmienną, znajdź drugą zmienną, porównaj
        return 0
# Usuń ostatnią tablicę zmiennych
z=0.1 # Dodaj trzeci element do tablicy zmiennych

class Klasa:  # Dodaj tablicę zmiennych Klasa do ostatniej tablicy zmiennych
    x = 1  # Dodaj Klasa.x
    class Klasa2:  # Dodaj tablicę zmiennych Klasa.Klasa2 do ostatniej tablicy zmiennych
        x = 2
        def fun(self, var1: int):
            return 2
    def _hello(self, var1: int):
        if x == var1:
            return 1

v = Klasa()